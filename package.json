{
  "name": "careero-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Repository wrapper — application source lives in Careero/",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npm --prefix Careero run dev",
    "build": "npm --prefix Careero ci && npm --prefix Careero run build",
    "vercel-build": "npm --prefix Careero ci && npm --prefix Careero run build",
    "check": "npm --prefix Careero run check",
    "test": "npm --prefix Careero test",
    "deploy:production": "npm --prefix Careero run deploy:production"
  }
}
